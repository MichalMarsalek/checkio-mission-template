def hq9plus(source_code):
    return ""

if __name__ == '__main__':
    #These "asserts" are used only for self-checking and aren't necessary for auto-testing
    assert hq9plus("h") == "Hello, world!"
    assert hq9plus("+q+") == "+q+"
    assert hq9plus("") == ""
    assert hq9plus("hq") == "Hello, world!\nhq"
